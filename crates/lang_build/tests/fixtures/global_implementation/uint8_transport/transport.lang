const let uint8 =
  (self, const let object: uint8): const + (compile || runtime):compile
    -> let result: uint8 =>
{
  object;
};

const let uint8 =
  (self, mut let object: uint8): const + (compile || runtime):compile
    -> let result: uint8 =>
{
  object;
};

mut let uint8 =
  (self, const let object: uint8): mut + (compile || runtime):compile
    -> let result: uint8 =>
{
  object;
};

mut let uint8 =
  (self, plain let object: uint8): mut + (compile || runtime):compile
    -> let result: uint8 =>
{
  object;
};

compile let identity =
  (self, const let object: uint8): compile
    -> let result: uint8 =>
{
  object;
};

public + compile let type_identity =
  (self, const let object: type): compile
    -> let result: type =>
{
  object;
};

mut let uint8 =
  (self, mut let object: uint8): mut + (compile || runtime):compile
    -> let result: uint8 =>
{
  object;
};
