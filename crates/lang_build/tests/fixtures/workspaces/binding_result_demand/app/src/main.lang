mut let choose =
  (self, t: type): const + compile
    -> let r: type =>
{
  t;
};

const let choose =
  (self, t: type): mut + compile
    -> let r: type =>
{
  t;
};

mut let Selected: type = uint8 choose;
